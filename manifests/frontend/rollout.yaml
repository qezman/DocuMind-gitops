apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: documind-frontend
  namespace: documind
spec:
  replicas: 2
  selector:
    matchLabels:
      app: documind-frontend
  strategy:
    canary:
      steps:
        - setWeight: 25
        - pause: { duration: 30s }
        - setWeight: 50
        - pause: { duration: 30s }
        - setWeight: 100
  template:
    metadata:
      labels:
        app: documind-frontend
    spec:
      containers:
        - name: frontend
          image: 332130072211.dkr.ecr.us-east-1.amazonaws.com/documind-frontend:d1c71f4b166513c8ce2b88f76e12d8599093fe10
          ports:
            - containerPort: 80
