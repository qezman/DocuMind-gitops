apiVersion: v1
kind: Service
metadata:
  name: documind-frontend
  namespace: documind
spec:
  type: ClusterIP
  selector:
    app: documind-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
