apiVersion: v1
kind: Service
metadata:
  name: documind-backend
  namespace: documind
spec:
  type: ClusterIP
  selector:
    app: documind-backend
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP