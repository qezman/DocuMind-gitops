apiVersion: v1
kind: Service
metadata:
  name: documind-agent
  namespace: documind
spec:
  type: ClusterIP # internal only, no Ingress
  selector:
    app: documind-agent
  ports:
    - port: 3003
      targetPort: 3003